import styled from "styled-components";

interface Props {
  className?: string;
}

const StyledProductSectionWrapper = styled.div`
  width: 100%;
  background-color: var(--producSection-mainColor);
`;

export default StyledProductSectionWrapper;